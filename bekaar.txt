DO NOT CHANGE THE RECOVERY WALL DESIGN.

The wall is now almost correct.

There is ONLY ONE remaining visual glitch:

On the current viewport, the bottom/right side of the second row becomes empty after a few cards.

See the screenshot:
the bottom row has cards on the left, but a large blank black area appears on the right.

FIX ONLY THIS.

==================================================
ROOT CAUSE
==================================================

EditorialWall currently creates only:

[...data, ...data, ...data]

and then splits that into topRow and bottomRow.

When the actual recovery data contains only a few cases, the resulting visual track is not long enough to continuously cover the viewport during the marquee animation.

==================================================
FIX
==================================================

Keep the exact same two-row marquee architecture.

Keep:

- top row moving LEFT
- bottom row moving RIGHT
- current card design
- current card sizes
- current rotations
- current spacing
- current animation speeds
- current edge fade
- current recovery terminology
- /recovery/${talent.slug}

ONLY make the visual track long enough that neither row can expose empty space.

Use enough VISUAL repetitions of the actual data to guarantee that the track is wider than the viewport.

For example, instead of only:

return [...data, ...data, ...data];

use enough repetitions, such as:

return [...data, ...data, ...data, ...data, ...data, ...data];

OR implement an equivalent robust calculation based on viewport width.

IMPORTANT:

This is visual duplication for the marquee loop only.

Do NOT create duplicate database records.

Do NOT show duplicate cards as a static grid.

Do NOT add Load More.

Do NOT change the layout.

==================================================
MOST IMPORTANT
==================================================

The wall must NEVER show:

[ CARD ][ CARD ][ CARD ]
                         HUGE EMPTY SPACE

Both rows must remain visually populated across the entire viewport throughout the animation.

The transition must remain seamless.

Do NOT change the animation direction or timing.

Do NOT redesign anything.

==================================================
VERIFY
==================================================

Test the wall when there are only:

3 real recovery cases
4 real recovery cases
6 real recovery cases
8+ real recovery cases

Even with only 3 real cases, both moving rows must continuously cover the full viewport with no visible empty region.

ONLY FIX THIS ONE GLITCH.

Do not touch any other section or animation.