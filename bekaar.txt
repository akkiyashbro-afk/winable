IMPORTANT: The current WinsAble website is now visually smooth and the existing animations feel good.
DO NOT redesign the website.
DO NOT replace the current visual system.
DO NOT touch the Hero, typography, section animations, scroll animations, or general dark/gold theme.

We only need to polish TWO things.

==================================================
1. KEEP THE REAL FINAL CTA BUTTON
==================================================

The "Start a Case" button inside the Final CTA section is CORRECT and must remain.

It should continue linking to:

#case-form

DO NOT remove this actual CTA.

However, there must NOT be a separate automatic/sticky floating "Start a Case" button appearing while the user scrolls through the page.

Search the entire project for any StickyCta / floating CTA / fixed bottom CTA implementation.

If any automatic floating CTA still exists:
- remove it from the homepage
- do not replace it with another floating button
- keep the normal Final CTA button
- keep the navbar Start a Case button
- keep the Hero Start Your Case button

The user should only see CTA buttons in their intended sections.

==================================================
2. REDUCE THE WALL SIDE BLUR
==================================================

The current EditorialWall has very strong left/right edge masks.

The wall currently uses:
- approximately 25% mask width
- backdrop-filter blur around 12px
- heavy gradient opacity

This makes too much of the wall disappear and reduces the visual impact of the cards.

Keep the current wall layout and animation EXACTLY as it is.

ONLY reduce the edge masking intensity.

Target result:
- central cards remain completely sharp and readable
- side cards still fade naturally into the background
- much less blur
- much less dark overlay
- smooth premium edge transition
- no harsh visible mask boundary

Recommended values:
- edge mask width: approximately 12%–15%
- min width can be reduced appropriately
- backdrop-filter blur: approximately 4px–6px
- reduce gradient opacity substantially
- do not blur the entire cards
- do not change card sizing
- do not change the moving animation
- do not change row direction
- do not change spacing unless absolutely necessary

The wall should feel like:
"cards fading into atmosphere"

NOT:
"cards being hidden behind a giant blur."

==================================================
3. DO NOT CHANGE THE WALL CONCEPT YET
==================================================

Do NOT migrate the old v2 wall yet.

Do NOT introduce Recovery Galaxy yet.

Do NOT import old v2 review data yet.

Do NOT redesign the wall into another concept.

Keep the current wall implementation for now.

Later we will separately decide what content comes from v2.

==================================================
4. KEEP THE WEBSITE SMOOTH
==================================================

The current animations are already very smooth.

Preserve:
- Reveal animations
- scroll animations
- text animations
- hero motion
- hover interactions
- section transitions
- marquee/moving wall animation
- current premium dark/gold appearance

Do not simplify or remove animations just to make the code smaller.

==================================================
5. VERIFY AFTER THE CHANGE
==================================================

Check:

Desktop:
- wall edges are less blurred
- cards are easier to see
- no floating CTA while scrolling
- Final CTA button still exists

Mobile:
- same improvements
- no layout break
- no excessive edge blur

Also search the whole repo for:
- StickyCta
- fixed bottom CTA
- "Start a Case" + fixed
- "Exclusive"
- "/exclusive/"

Report what was found and what was changed.

Do not make any unrelated changes.