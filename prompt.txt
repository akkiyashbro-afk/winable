The deployment/build succeeded, but the LIVE Vercel Admin Panel still shows:

Recoveries (0)
Failed to fetch recoveries

Do NOT make another UI change.

I need you to diagnose the ACTUAL runtime MongoDB failure.

The latest recovery implementation already uses MongoDB + idempotent upsert seeding from recoveredProfiles. Do not replace it with mock data or localStorage.

DO THIS NOW:

1. Run the app locally.
2. Verify that MONGODB_URI is actually loaded by the server runtime.
3. Test the MongoDB connection directly.
4. Test getRecoveriesFn() directly.
5. Verify the RecoveryModel can read/write.
6. Verify the recoveredProfiles seed/upsert runs.
7. Verify 36 recovery records exist after the first successful connection.
8. If MongoDB connection fails, expose a useful SERVER-SIDE diagnostic error instead of the generic "Failed to fetch recoveries".
9. Never expose the actual MONGODB_URI/password in the browser, logs visible to users, or GitHub.
10. Check whether the problem is:
   - missing MONGODB_URI
   - wrong MongoDB URI
   - wrong database-user password
   - special characters in password not URL encoded
   - MongoDB Atlas Network Access/IP restriction
   - MongoDB user permissions
   - connection timeout
   - Mongoose/server runtime issue
   - Vercel environment variable not available to Production
   - wrong deployment/environment
11. Check Vercel Production environment specifically.
12. Do NOT assume that "deployment successful" means MongoDB works.
13. Do NOT silently fall back to fake data in Admin.
14. Do NOT change the public Recovery Wall design.

IMPORTANT:

The Vercel deployment must have these server environment variables:

MONGODB_URI=[I WILL PROVIDE THIS]
RESEND_API_KEY=[I WILL PROVIDE THIS]
CASE_RECEIVER_EMAIL=[I WILL PROVIDE THIS]
FROM_EMAIL=[I WILL PROVIDE THIS]

Do not hardcode them.

After diagnosing the issue, tell me EXACTLY what is failing and what I need to change.

Then fix the code if the issue is code-related.

If the issue is Vercel/Atlas configuration, do not fake a code fix. Tell me the exact configuration step required.

After fixing:

1. local MongoDB connection test
2. local /admin test
3. verify Recoveries count
4. verify all 36 profiles
5. edit one recovery
6. reload and verify persistence
7. verify public Recovery Wall gets the same updated data
8. deploy to Vercel
9. verify production /admin
10. verify production Recovery Wall

Do not report success until the production /admin page actually loads the recovery records.